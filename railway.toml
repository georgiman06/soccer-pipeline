[build]
builder = "NIXPACKS"

[deploy]
startCommand = "cd pipeline && gunicorn server:app -b 0.0.0.0:$PORT -w 1 --threads 4 --timeout 120"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
